{
  "name": "aegis-dao-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "AEGIS DAO governance toolkit - SDK, CLI, and dApp",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "test:contract": "npx ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.32.1",
    "@solana/spl-token": "^0.4.0",
    "@solana/web3.js": "^1.95.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^10.0.0",
    "turbo": "^2.3.0",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
